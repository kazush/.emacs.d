#+title: Emacs pre init configuration
#+startup: content indent
#+property: header-args :tangle yes

#+begin_src emacs-lisp
(message "Loading preinit config...")
#+end_src

* WSL

#+begin_src emacs-lisp
(defun my/is-wsl ()
  (file-exists-p "/dev/dxg"))
#+end_src

** maximize frame

#+begin_src emacs-lisp
(when (my/is-wsl)
  (unless (daemonp)
    (toggle-frame-maximized)))
#+end_src
